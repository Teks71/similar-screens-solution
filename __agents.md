## 📌 **Общий контекст**

Этот репозиторий — **Python монорепозиторий на uv-workspace**, содержащий три связанных проекта:

* `contracts` — общие Pydantic-контракты (источник истины).
* `backend-service` — FastAPI backend.
* `telegram-bot` — Telegram-бот, использующий те же контракты.
* Корень содержит `pyproject.toml`, определяющий uv-workspace.

Агенты обязаны учитывать структуру и следовать этим правилам.

---

# 📦 **Структура репозитория (уровень workspace)**

```
similar-screens-solution/
│
├─ pyproject.toml              # объявляет uv-workspace + sources
│
├─ contracts/                  # общие модели
│   ├─ pyproject.toml
│   └─ contracts/
│       ├─ __init__.py
│       └─ dto.py
│
├─ backend-service/            # backend на FastAPI
│   ├─ pyproject.toml
│   └─ backend_service/
│       ├─ __init__.py
│       └─ app.py
│
└─ telegram-bot/               # Telegram-бот
    ├─ pyproject.toml
    └─ telegram_bot/
        ├─ __init__.py
        └─ main.py
```

---

# 🧩 **Связи внутри репозитория**

### 1. **projects** → используют `contracts`

Оба проекта (`backend-service` и `telegram-bot`) зависят от:

```toml
dependencies = ["contracts"]
```

Не использовать `file:` или прямые пути.
Все зависимости разрешает uv через workspace.

### 2. **contracts — единый источник истины**

Любые модели, DTO, структуры запросов/ответов должны создаваться и изменяться в:

```
contracts/contracts/
```
каждая модель в отдельном файле. Допускается определять вложенные модели в одном файле если они используются только в одном месте. Допускается определять локальные перечисления и константы в том же файле если также используются только с этой моделью

Агент должен:

* расширять или изменять модели **только в этом проекте**;
* не дублировать модели в backend-service или telegram-bot;
* при изменении контракта — корректировать оба потребителя.

---

# 🛠 **Правила изменения кода**

## 1. **Добавление новых API / контрактов**

* Сначала редактировать Pydantic-модели в `contracts/`.
* Затем адаптировать backend-service.
* Затем адаптировать telegram-bot.

## 2. **Запрещено**

* Создавать новые пакеты вне workspace-проекта.
* Создавать дублирующие модели в `backend_service` или `telegram_bot`.
* Добавлять произвольные зависимости без обновления соответствующего `pyproject.toml`.
* Нарушать workspace-структуру.

## 3. **Создание новых файлов**

* Backend-логика — только в `backend_service/`.
* Логика бота — только в `telegram_bot/`.
* Общие модели — строго в `contracts/`.

---

# 🚀 **Как агенты должны запускать код**

## Запуск backend-сервиса:

```
uv run --package backend-service uvicorn backend_service.app:app --reload
```

## Запуск Telegram-бота:

```
uv run --package telegram-bot python -m telegram_bot.main
```

---

# 📐 **Стандарты кода**

### Backend

* FastAPI
* Pydantic v2
* Чёткие response_model
* Strict typing

### Contracts

* Только Pydantic-модели
* Никакой логики, только структуры данных
* Импортируются как:

```python
from contracts.dto import SomeModel
```

### Telegram-bot

* `aiogram>=3`
* Вся бизнес-логика использует модели из `contracts`
* HTTP-вызовы к backend выполняются через `httpx`

---

# ✨ **Главное правило**

> **Любые изменения в структурах данных или API должны начинаться с проекта `contracts`.
> Остальные два проекта обязаны подстраиваться под него.**